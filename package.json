{
  "name": "vulnerable-app",
  "version": "1.0.0",
  "description": "Intentionally vulnerable app for security testing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node test-vulnerabilities.js",
    "sonar": "sonar-scanner",
    "sonar:setup": "node scripts/setup-sonarqube.js",
    "lint": "eslint . --ext .js 2>/dev/null || true",
    "security-check": "npm audit --audit-level=moderate",
    "dependency-check": "node scripts/run-dependency-check.js",
    "dependency-check:docker": "docker run --rm -v $(pwd):/src owasp/dependency-check --scan /src --format JSON --project 'Vulnerable App'",
    "security-scan": "npm run security-check && npm run dependency-check",
    "full-scan": "npm run lint && npm run security-check && npm run sonar && npm run dependency-check"
  },
  "dependencies": {
    "express": "^4.18.2",
    "lodash": "3.10.1",
    "sqlite3": "^5.1.6",
    "body-parser": "^1.20.2",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "sonar-scanner": "^3.1.0",
    "eslint": "^8.40.0"
  }
}
